endpoints:
  token: /tokened
tokens:
  Issuer: http://localhost:5050
  Lifetime: 00:01:00
certificate_providers:
  file:
    certificate_path: certs/localhost.crt
    private_key_path: certs/localhost.key
users:
  - username: root
    plain_text_password: superuser
    acl: [admin]
  - username: default
    acl: [pull-any, push-any]
acl:
  - name: admin
    access:
      - type: registry
        name: '*'
        actions: ['*']
      - type: repository
        name: '**/*'
        actions: ['*']
  - name: pull-any
    access:
      - type: repository
        name: '*'
        actions: [pull]
